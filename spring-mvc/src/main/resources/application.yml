spring:
  application:
    name: spring-mvc
  output:
    ansi:
      enabled: always
  datasource:
    url: jdbc:h2:~/test
  jpa:
    properties:
      hibernate:
        # query? ?? ? ??? ?? ?? ?? ?
        format_sql: true
        highlight_sql: true
    hibernate:
      ddl-auto: update


logging:
  level:
    study.coroutine: debug
    # query? ?? ?? ??
    # org.hibernate.SQL: debug
    # org.hibernate.orm.jdbc.bind: trace